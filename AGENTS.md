# Agent Instructions (cc-multi-agent)

## Branching / Safety
- Do not make changes directly on `main`. Create a branch with prefix `codex/` first, then apply edits on that branch.
- If already on `main` with uncommitted changes, move the work to a new `codex/` branch (or revert) before continuing.

